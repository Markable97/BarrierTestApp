<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="ToolbarThemeOverlay" parent="">
        <item name="actionMenuTextColor">@color/white</item>
        <item name="textAllCaps">false</item>
        <item name="actionMenuTextAppearance">@style/Body4</item>
    </style>

    <style name="Widget.Toolbar" parent="Widget.MaterialComponents.Toolbar">
        <item name="navigationIconTint">@color/white</item>
        <item name="android:background">?attr/colorPrimaryVariant</item>
        <item name="titleTextAppearance">@style/Body1</item>
        <item name="titleTextColor">@color/white</item>
        <item name="materialThemeOverlay">@style/ToolbarThemeOverlay</item>
    </style>

    <style name="Button" parent="Widget.MaterialComponents.Button.UnelevatedButton">
        <item name="android:textSize">14sp</item>
        <item name="cornerRadius">4dp</item>
        <item name="fontFamily">@font/roboto</item>
        <item name="android:textAllCaps">false</item>
    </style>

</resources>